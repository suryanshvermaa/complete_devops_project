apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
  namespace: my-app
spec:
  selector:
    app: mongodb-deployment
  type: ClusterIP
  ports:
  - name: mongo-svc
    protocol: TCP
    port: 27017
    targetPort: 27017